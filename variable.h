#ifndef CONCURRENCYFLUID_VARIABLE_H
#define CONCURRENCYFLUID_VARIABLE_H

#include <bits/stdc++.h>

using namespace std;

constexpr size_t N = 36, M = 84;
// constexpr size_t N = 14, M = 5;
constexpr size_t T = 200;
constexpr std::array<pair<int, int>, 4> deltas{{{-1, 0}, {1, 0}, {0, -1}, {0, 1}}};

mt19937 rnd(1337);

#endif //CONCURRENCYFLUID_VARIABLE_H
